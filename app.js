(function(){
    
    let button = document.querySelector('.btn')
    let body = document.querySelector('body')
    let container = document.querySelector('.container')
    
    button.addEventListener('click', onclick){
        function(e).button(onclick, button).container{
            document.button(Math.floor('.container'))
            let container.button(Math.random(length))
            addEventListener(onclick, button(random),length(button, Function(e)))
            length(button)
        }
    }
})